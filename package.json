{
  "name": "e-quaart",
  "private": true,
  "version": "0.1.0",
  "description": "E-QuAART — Electronic Quality Assured Assessment Result Tool",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "start": "npm run start --workspace server",
    "test": "npm test --workspace server && npm test --workspace client",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace client",
    "db:migrate": "npm run db:migrate --workspace server",
    "db:seed": "npm run db:seed --workspace server",
    "create-admin": "npm run create-admin --workspace server --"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
